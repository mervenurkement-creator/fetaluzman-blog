/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['fetaluzman.com', 'images.unsplash.com'],
  },
  pageExtensions: ['ts', 'tsx', 'mdx'],
}
module.exports = nextConfig
